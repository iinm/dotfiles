#!/usr/bin/env bash

set -eu -o pipefail

usage="Usage: ${BASH_SOURCE[0]} METADATA_DIR"
metadata_dir="${1:?"$usage"}"

mkdir -pv "$metadata_dir/memory"
mkdir -pv "$metadata_dir/sandbox"
touch "$metadata_dir/instructions.md"
