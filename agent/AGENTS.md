## Implementation Workflow

- After modifying code, always run `npm run check` to verify lint, types, and tests before reporting completion.

## Commands

- Run lint, typecheck, and test: `npm run check`
- Fix lint errors: `npm run fix` or `npm run fix -- --unsafe`
